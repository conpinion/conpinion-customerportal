<div class="row">
  <div class="col-md-4 col-md-offset-4">
    <h2>
      <%= t 'views.users.edit_your_password' if @user == current_user %>
      <%= t 'views.users.edit_password' unless @user == current_user %>
    </h2>

    <%= semantic_form_for @user, validate: true do |f| %>
      <%= f.semantic_errors %>
      <% if @user == current_user %>
        <%= f.input :current_password, input_html: { autofocus: true } %>
      <% end %>
      <%= f.input :new_password, input_html: { autofocus: !current_user.has_role?(:admin)} %>
      <%= f.input :repeat_password %>
      <%= f.actions do %>
        <%= f.action :submit, label: t('views.users.update_password'), button_html: { class: 'btn btn-primary' } %>
        <%= link_to_back t('actions.cancel') %>
      <% end %>
    <% end %>
  </div>
</div>
